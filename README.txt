install packages with: 
pip install -r requirements.txt

then just run:
python f1_passing.py


results will be inserted into a folder "f1_passing_stats_demo", which will be created if it does not exist already